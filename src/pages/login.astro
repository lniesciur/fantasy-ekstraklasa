---
import Layout from "../layouts/Layout.astro";
import AuthForm from "../components/AuthForm.tsx";
---

<Layout title="Login - Fantasy Ekstraklasa">
  <div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-background to-primary/10 p-4">
    <div class="max-w-md w-full space-y-8">
      <div class="text-center">
        <h1 class="text-3xl font-bold text-primary mb-2">Fantasy Ekstraklasa</h1>
        <p class="text-muted-foreground">Sign in to your account</p>
      </div>

      <!-- AuthForm component -->
      <div class="bg-card backdrop-blur-lg rounded-lg p-8 border border-border shadow-lg">
        <AuthForm
          client:load
          onSubmit={(email, password) => {
            // TODO: Implement actual login logic
            // eslint-disable-next-line no-console
            console.log("Login attempt:", { email, password });
            void email;
            void password;
          }}
        />
      </div>
    </div>
  </div>
</Layout>
